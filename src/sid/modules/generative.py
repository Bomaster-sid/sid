import google.generativeai as genai\ndef configurar_api(api_key):\n    try:\n        genai.configure(api_key=api_key)\n        return True\n    except: return False\ndef gerar_texto(prompt, instrucao='...'):\n    try:\n        model = genai.GenerativeModel('gemini-1.5-flash-latest', system_instruction=instrucao)\n        return model.generate_content(prompt).text.strip()\n    except Exception as e: return f'Erro: {e}'\ndef gerar_codigo(pedido):\n    instrucao = 'Gere APENAS código Python.'\n    return gerar_texto(pedido, instrucao_sistema=instrucao)