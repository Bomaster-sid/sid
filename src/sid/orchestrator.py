from .modules import matematica, generative\ndef delegar_tarefa_sid(prompt):\n    pl = prompt.lower()\n    if any(k in pl for k in ['calcule', 'resolva']):\n        exp = pl.replace('calcule','').strip()\n        return {'tipo': 'matematica', 'resultado': matematica.resolver_expressao_matematica(exp)}\n    elif any(k in pl for k in ['crie um código', 'escreva um script']):\n        return {'tipo': 'codigo', 'resultado': generative.gerar_codigo(prompt)}\n    else:\n        return {'tipo': 'geral', 'resultado': generative.gerar_texto(prompt)}